eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: false
  # eurekaServer自我保护机制
  server:
    enable-self-preservation: false

spring:
  application:
    name: eureka

server:
  port: 8761